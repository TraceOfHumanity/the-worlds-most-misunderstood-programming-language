// indexOf метод повертає індекс першого входження заданого елемента в масив
// якщо елемент не знайдений, то повертає -1

const array = [1, 2, 3, 4, 5];
console.log(array.indexOf(3));

//--------------------------------

const array1 = [1, 2, 3, 4, 5];
console.log(array1.indexOf(6));
